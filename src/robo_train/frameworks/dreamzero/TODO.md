# DreamZero Placeholder

TODO: add a DreamZero/WAM framework plugin after the real backend command,
profile schema, dataset contract, and checkpoint format are defined.

Expected future shape:

- `profile_schema.py`
- `loader.py`
- `converter.py`
- `launcher.py`
- `plugin.py`

Do not register a partial DreamZero plugin in `FRAMEWORK_REGISTRY` until
`--dry-run` and real launch behavior are both specified.
