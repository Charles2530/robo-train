# DreamZero Config Placeholder

TODO: add `base/` and `tasks/` YAML profiles when the DreamZero/WAM backend
contract is ready. Profiles should resolve through `TrainingProfile.default_world_model()`
and use `WorldModelDataView`.
