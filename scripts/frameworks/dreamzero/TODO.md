# DreamZero Script Placeholder

TODO: add shell wrappers only after a real DreamZero/WAM backend command exists.
Wrappers should call `python -m robo_train.cli.train --framework dreamzero ...`
through the shared CLI and should not contain backend-specific Python logic.
